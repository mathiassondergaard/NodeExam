const controller = require('./logging-controller');
const service = require('./logging-service');
const router = require('./logging-routes');

module.exports = {
    controller,
    service,
    router
};