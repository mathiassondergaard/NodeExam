// return filename.substring(filename.lastIndexOf('.')+1, filename.length) || filename; for file extension check
// remember for download:
    // res.setHeader("Content-Type", "text/csv");
    // res.setHeader("Content-Disposition", "attachment; filename=whatever.csv");
